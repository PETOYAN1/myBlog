export const metadata = {
    title: "Contacts",
    description: "Get in touch with our team for projects or inquiries."
};

export default function ContactsLayout({ children }) {
    return <>{children}</>;
}
